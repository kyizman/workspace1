import java.util.Scanner;
public class DrivingCost {


	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		Scanner input = new Scanner(System.in);
	System.out.println("Enter a number that is less than 1000 (positive)");
	int x = input.nextInt();
	if (x>999) if (x<0); {
	// set a message to prompt user that that number will not work
	System.out.println("That number will not work");
	
	 int sum = 0;
	while (x !=0);
	
	sum = x % 10;
 x /=10;
 
 System.out.println(" the sum of the diggits is" + sum);
	}
	}
}
